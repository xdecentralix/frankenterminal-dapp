import { Html, Head, Main, NextScript } from "next/document";

export default function Document() {
	return (
		<Html lang="en">
			<Head />
			<body className="font-default w-full bg-layout-primary text-text-primary font-medium">
				<Main />
				<NextScript />
			</body>
		</Html>
	);
}
